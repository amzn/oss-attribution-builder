version: '2'

services:
  web:
    build: .
    ports:
    - 2424:2424
    links:
    - database
    environment:
    - USER
  database:
    image: postgres
    volumes:
    - ./sql:/docker-entrypoint-initdb.d
